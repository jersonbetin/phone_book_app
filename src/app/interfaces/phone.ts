export interface IPhone {
  key?: string;
  firstName?: string;
  lastName?: string;
  phone?: string;
}

export interface IErrorPhone {
  firstName: boolean;
  lastName: boolean;
  phone: boolean;
}
